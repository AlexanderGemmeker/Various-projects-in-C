#include <stdio.h>

int main(void)
{
    // arithmetic operators = + - * / % ++ --
    float x = 2;
    float y = 3;
    float z = 0;

    // z = x + y;
    // z = x - y;
    // z = x * y;
    z = x / y;

    printf("%f\n", z);

    return 0;
}