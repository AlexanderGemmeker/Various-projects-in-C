#include <stdio.h>
#include <math.h>

int main(void)
{
    // int x = 2;
    float x = 3.99;

    //x = sqrt(x);
    //x = pow(x, 3);
    //x = round(x);
    //x = ceil(x);
    //x = floor(x);


    printf("%f", x);

    return 0;

    //
}