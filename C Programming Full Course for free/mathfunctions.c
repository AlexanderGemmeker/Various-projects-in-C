#include <stdio.h>
#include <math.h>

int main(void)
{
    // int x = 2;
    float x = 45;

    //x = sqrt(x);
    //x = pow(x, 3);
    //x = round(x);
    //x = ceil(x);
    //x = floor(x);
    //x = abs(x);
    //x = log(x);
    //x = sin(x); // Trigonometry
    //x = cos(x);
    x = tan(x);

    printf("%f", x);

    return 0;

    //
}