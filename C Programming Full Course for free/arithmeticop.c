#include <stdio.h>

int main(void)
{
    // arithmetic operators = + - * / % ++ --
    int x = 2;
    int y = 3;
    float z = 0;

    // z = x + y;
    // z = x - y;
    // z = x * y;
    // z = x / y;

    printf("%f\n", z);

    return 0;
}